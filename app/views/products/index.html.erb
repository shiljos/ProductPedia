
<h1>Products</h1>
  <ul>
    <% @products.each do |product| %>
      <li>
      	<%= product.name %>
        <%= link_to('View Product', product_path(product.id))%>
      </li>    
    <%end%>
  </ul>